name: stage
db_uri: "postgresql://stage:${STAGE_DB_PASSWORD:-secure}@host/stage"
debug: false
log_level: INFO
random_seed: 2024
data:
  price_csv: /mnt/stage_data/prices.csv
  price_column: price
analytics:
  window: 256
  bins: 48
  delta: 0.005
tracking:
  enabled: true
  base_dir: /mnt/stage_runs
